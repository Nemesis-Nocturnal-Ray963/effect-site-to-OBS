import React from "react";
import { PlaceholderPage } from "./PlaceholderPage";

export function ObsConnectionPage(): React.ReactElement {
  return <PlaceholderPage title="OBS" eyebrow="Connections" message="OBS WebSocket connection is planned." />;
}
