import React from "react";
import { PlaceholderPage } from "./PlaceholderPage";

export function MacrosPage(): React.ReactElement {
  return <PlaceholderPage title="Macros" eyebrow="Effects" message="Macro management is planned." />;
}
